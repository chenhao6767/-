@rem Gradle wrapper for Windows
@if "%DEBUG%"=="" @echo off
@rem Set local scope for the variables with windows NT shell
if "%OS%"=="Windows_NT" setlocal
set DIRNAME=%~dp0
"%DIRNAME%gradle\wrapper\gradlew.bat" %*
